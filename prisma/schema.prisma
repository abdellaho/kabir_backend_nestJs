// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Ville {
  id          					        BigInt         			              @id @default(autoincrement())
  nomVille       				        String      
  etablissements  				      Etablissement[]
  repertoires   				        Repertoire[]
  fournisseurs   				        Fournisseur[]
}

model Fournisseur {
  id              				      BigInt    					              @id @default(autoincrement()) 
  designation		 			          String
  type  				                Int
  tel1             				      String
  tel2             				      String
  ice                           String
  archiver              		    Boolean @default(false)
  supprimer              		    Boolean @default(false)
  ville           				      Ville?     				                @relation(fields: [villeId], references: [id])
  villeId         				      BigInt?
  stocks           			        Stock[]
}

model Personnel {
  id              				      BigInt    					              @id @default(autoincrement())
  designation             			String
  cin            				        String
  login          				        String
  password       				        String
  typePersonnel   				      Int
  etatComptePersonnel 			    Boolean @default(false)
  tel1             				      String
  tel2             				      String
  adresse         				      String
  email           				      String
  dateEntrer      				      DateTime @db.Date
  salaire         				      Decimal @db.Decimal(8, 2)
  archiver              		    Boolean @default(false)
  supprimer              		    Boolean @default(false)
  absencesOperation         		Absence[] @relation("personnelOperationAbsence")
  absences         				      Absence[] @relation("personnelAbsence")
  repertoires      				      Repertoire[]  				          @relation
}

model Voiture {
  id              				      BigInt    					              @id @default(autoincrement())
  numVoiture       				      String
  kmMax    				              Int
}

model Etablissement {
  id              				      BigInt    			 		              @id @default(autoincrement())
  nom             				      String
  cheminBD        				      String
  tel1             				      String
  tel2             				      String
  tel3             				      String
  fax             				      String
  gsm             				      String
  email           				      String
  siteweb         				      String
  cnss            				      String
  patente         				      String
  adresse         				      String
  raisonSocial    				      String
  ice             				      String
  ife             				      String
  rc              				      String
  port            				      Int
  hostMail        				      String
  paswordMail     				      String
  paswordMailFake 				      String
  fromMail        				      String
  userMail        				      String
  capitale        				      Int
  pourcentageLiv  				      Decimal
  lienDbDump      			    	  String
  lienBackupDB   			 	        String
  lundi           				      Boolean @default(false)
  mardi           				      Boolean @default(false)
  mercredi        				      Boolean @default(false)
  jeudi           				      Boolean @default(false)
  vendredi        				      Boolean @default(false)
  samedi          				      Boolean @default(false)
  dimanche        				      Boolean @default(false)
  dateTime        				      DateTime
  typeExec        				      Int
  numJour         				      Int
  ville           				      Ville?     				            @relation(fields: [villeId], references: [id])
  villeId  						          BigInt?
}

model Prime {
  id              				      BigInt    					            @id @default(autoincrement())
  montant         				      Decimal
  prime           				      Decimal
}

model ParamCommiss {
  id              				      BigInt    					            @id @default(autoincrement())
  mntDepart         			      Decimal
  mntFin           				      Decimal
  pourc           				      Decimal
}

model Repertoire {
   id              				      BigInt    					           	@id @default(autoincrement())
   designation		 			        String
   tel1             			      String
   tel2             			      String
   tel3             			      String
   adresse         				      String
   email           				      String
   typeRepertoire   			      Int
   typeReglment     			      Int
   ife             				      String
   ice             				      String
   archiver         				    Boolean @default(false)
   bloquer         				      Boolean @default(false)
   sysDate         				      DateTime
   observation     				      String
   nbrOperationClient 			    Int
   plafond   			              Decimal
   personnel         			      Personnel?  			          @relation(fields: [personneld], references: [id])
   personneld    				        BigInt?
   ville           				      Ville?     				          @relation(fields: [villeId], references: [id])
   villeId         				      BigInt?
   employes         			      Employe[]
   bonSorties       			      BonSortie[]
   bulttinPais      			      BulttinPai[]
   cheques         				      Cheque[]
   comptas         				      Compta[]
   comptabilites   				      Comptabilite[]
   importations					        Importations[]
   livraisonCommercials 			  Livraison[] @relation("commercialLivraison")
   livraisonCommercialAnciens   Livraison[] @relation("commercialAncienLivraison")
   livraisonClients 				    Livraison[] @relation("clientLivraison")
}

model Stock {
  id              				      BigInt    					            @id @default(autoincrement())
  designation         			   	String
  sysDate             			    DateTime
  pahtGrossiste           		  Decimal
  prixCommercial           		 	Decimal
  tva           				        Decimal
  pattc           				      Decimal
  pvttc           				      Decimal
  pvaht           				      Decimal
  benifice           			      Decimal
  qteStock        				      Int
  qtePVMin1        				      Int
  qtePVMin2        				      Int
  qtePVMin3        				      Int
  qtePVMin4        				      Int
  qteFacturer        			      Int
  prixVentMin1           		    Decimal
  prixVentMin2           		    Decimal
  prixVentMin3           		    Decimal
  prixVentMin4          	 	    Decimal
  remiseMax1             		    Decimal
  remiseMax2           			    Decimal
  remiseMax3           			    Decimal
  remiseMax4           			    Decimal
  prixImport            		    Decimal
  commission            		    Decimal
  archiver              		    Boolean @default(false)
  supprimer              		    Boolean @default(false)
  qteStockImport              	Int
  montant1       			          Decimal
  montant2       			          Decimal
  montant3       			          Decimal
  prime1       			            Decimal
  prime2       			            Decimal
  prime3       			            Decimal
  fournisseur        			      Fournisseur          	        @relation(fields: [fournisseurId], references: [id])
  fournisseurId      			      BigInt
  detAchatFactures   			      DetAchatFacture[]
  detAchatLivraisons 			      DetAchatLivraison[]
  detFactures        			      DetFacture[]
  detImportations    			      DetImportations[]
  detLivraisons      			      DetLivraison[]
  detailBonSorties   			      DetailBonSortie[]
  detBulttinPais	  			      DetBulttinPai[]
}

model Employe {
  id              				      BigInt    					              @id @default(autoincrement())
  numEmp           				      Int
  livrerNonLivrerDroit 			    Int
  codeEmp         				      String
  nom             				      String
  prenom          				      String
  login           				      String
  motpass         				      String
  email           				      String
  typeUser        				      Int 
  etatCompte      				      Boolean @default(false)
  validationMnt   				      Decimal
  motPassFake     				      String
  commercial      				      Boolean @default(false)
  pvLibre         				      Boolean @default(false)
  gerant          				      Boolean @default(false)
  magasinier      				      Boolean @default(false)
  bulltinPaie     				      Boolean @default(false)
  imprimStockSimple 			      Boolean @default(false)
  livraisonLimite 				      Boolean @default(false)
  repertoire      				      Repertoire  			            @relation(fields: [repertoireId], references: [id])
  repertoireId    				      BigInt
  bonSorties       			        BonSortie[]
  bulttinPais      			      	BulttinPai[]
  cheques         				      Cheque[]
  comptabilites   				      Comptabilite[]
  importations    				      Importations[]
  livraisons	  				        Livraison[]
}

model Droit {
  id              				        BigInt    					            @id @default(autoincrement())
  listRepertoire  				        Boolean @default(false)
  listStock       				        Boolean @default(false)
  listLivraison   				        Boolean @default(false)
  listFacture     				        Boolean @default(false)
  listAchatLivraison 			        Boolean @default(false)
  listAchatFacture 				        Boolean @default(false)
  listAchatLivraisonEtranger   	  Boolean @default(false)
  ajouterRepertoire 			        Boolean @default(false)
  ajouterStock     				        Boolean @default(false)
  ajouterLivraison  			        Boolean @default(false)
  ajouterFacture    			        Boolean @default(false)
  ajouterAchatLivraison 		      Boolean @default(false)
  ajouterAchatFacture 			      Boolean @default(false)
  ajouterAchatLivraisonEtranger   Boolean @default(false)
  modifRepertoire   			        Boolean @default(false)
  modifStock       				        Boolean @default(false)
  modifLivraison   				        Boolean @default(false)
  modifFacture     				        Boolean @default(false)
  modifAchatLivraison 			      Boolean @default(false)
  modifAchatFacture 			        Boolean @default(false)
  modifAchatLivraisonEtranger     Boolean @default(false)
  suppRepertoire   				        Boolean @default(false)
  suppStock       				        Boolean @default(false)
  suppLivraison   				        Boolean @default(false)
  suppFacture     				        Boolean @default(false)
  suppAchatLivraison 			        Boolean @default(false)
  suppAchatFacture 				        Boolean @default(false)
  suppAchatLivraisonEtranger   	  Boolean @default(false)
  etatRepertoire   				        Boolean @default(false)
  etatStock       				        Boolean @default(false)
  etatLivraison   				        Boolean @default(false)
  etatFacture     				        Boolean @default(false)
  etatAchatLivraison 			        Boolean @default(false)
  etatAchatFacture 				        Boolean @default(false)
  etatAchatLivraisonEtranger   	  Boolean @default(false)
  imprimeRepertoire 			        Boolean @default(false)
  imprimeStock     				        Boolean @default(false)
  imprimeLivraison  			        Boolean @default(false)
  imprimeFacture    			        Boolean @default(false)
  imprimerAchatLivraison 		     	Boolean @default(false)
  imprimerAchatFacture 			      Boolean @default(false)
  imprimerAchatLivraisonEtranger  Boolean @default(false)
}

model Absence {
  id              				      	BigInt    					            @id @default(autoincrement())
  dateAbsence          				    DateTime @db.Date
  matin          				      	  Boolean @default(false)
  apresMidi      				      	  Boolean @default(false)
  dateOperation  				      	  DateTime
  personnelOperation  				    Personnel?     				      	  @relation(name: "personnelOperationAbsence", fields: [personnelOperationId], references: [id])
  personnelOperationId  			    BigInt?
  personnel     				      	  Personnel  			                @relation(name: "personnelAbsence", fields: [personnelId], references: [id])
  personnelId   				      	  BigInt
}

model AchatFacture {
  id              				      	BigInt    					            @id @default(autoincrement())
  codeAF         				      	  String
  numAchat       				      	  Int
  dateAF         				      	  DateTime @db.Date
  sysDate       				      	  DateTime
  dateAvances   				      	  DateTime @db.Date
  mantantAvancs  				      	  Decimal
  prixNormalAchatHt  				      Int
  numeroFacExterne  				      String
  numeroIF      				      	  String
  mantantAF      				      	  Decimal
  mantantBFBenefice  				      Decimal
  montantNonTaxable  				      Decimal
  dateReglement  				      	  DateTime @db.Date
  typeReglment   				      	  Int
  typePaiement   				      	  String
  numCheque      				      	  String
  mantantTotHT   				      	  Decimal
  mantantTotHTVA  				      	Decimal
  mantantTotTTC  				      	  Decimal
  tva20         				      	  Decimal
  tva7          				      	  Decimal
  tvaArbtraire  				      	  Decimal
  manuelAutoMatique  				      Int
  mntManuelTva7  				      	  Decimal
  mntManuelTva10  				      	Decimal
  mntManuelTva12  				      	Decimal
  mntManuelTva13  				      	Decimal
  mntManuelTva14  				      	Decimal
  mntManuelTva20  				      	Decimal
  montantTVA7   				      	  Decimal
  montantTVA10  				      	  Decimal
  montantTVA12  				      	  Decimal
  montantTVA13  				      	  Decimal
  montantTVA14  				      	  Decimal
  montantTVA20  				      	  Decimal
  mntHtTVA7     				      	  Decimal
  mntHtTVA10    				      	  Decimal
  mntHtTVA12    				      	  Decimal
  mntHtTVA13    				      	  Decimal
  mntHtTVA14    				      	  Decimal
  mntHtTVA20    				      	  Decimal
  mntTtcTVA7    				      	  Decimal
  mntTtcTVA10   				      	  Decimal
  mntTtcTVA12   				      	  Decimal
  mntTtcTVA13   				      	  Decimal
  mntTtcTVA14   				      	  Decimal
  mntTtcTVA20   				      	  Decimal
  mntTtc        				      	  Decimal
  montantDroitSupplementaire  		Decimal
  disabledHT    				      	  Boolean @default(false)
  disabledManuel  				      	Boolean @default(false)
  detAchatFactures  				      DetAchatFacture[]
}

model AchatLivraison {
  id              				      	BigInt    					           @id @default(autoincrement())
  codeAL        				      	  String
  numAchat       				      	  Int
  dateAL         				      	  DateTime @db.Date
  sysDate       				      	  DateTime
  prixNormalAchatHt  				      Int
  numeroBlExterne  				    	  String
  mantantAL      				      	  Decimal
  mantantALTTC   				      	  Decimal
  mantantALTVA20  				      	Decimal
  mantantALTVA7   				      	Decimal
  totalMantantALTVA  				      Decimal
  mantantBFBenefice  				      Decimal
  montantTVA7   				      	  Decimal
  montantTVA10  				      	  Decimal
  montantTVA14  				      	  Decimal
  montantTVA20  				      	  Decimal
  detAchatLivraisons              DetAchatLivraison[]
}

model Balance {
  id              				      	BigInt    					           @id @default(autoincrement())
  dateBalance          				    DateTime @db.Date
  nature        				      	  String
  debitPrec     				      	  Decimal
  creditPrec    				      	  Decimal
  debit         				      	  Decimal
  credit        				      	  Decimal
  soldeDebiteur  				      	  Decimal
  soldeCrediteur  				      	Decimal
  planComptable  				      	  PlanComptable  			          @relation(fields: [planComptableId], references: [id])
  planComptableId  			      		BigInt
}

model Bilan {
  id              				      	BigInt    					           @id @default(autoincrement())
  date1         				      	  DateTime @db.Date
  date2         				      	  DateTime @db.Date
  stock        				      		  Decimal
  banque       				      		  Decimal
  caisse1      				      		  Decimal
  caisse2      				      		  Decimal
  actifDivers  				      		  Decimal
  capital      				      		  Decimal
  dgi          				      		  Decimal
  cnss         				      		  Decimal
  loyer        				      		  Decimal
  salaire      				      		  Decimal
  tva          				      		  Decimal
  chiffreAffaire  				      	Decimal
  passifDivers  				      	  Decimal
  compteCourant  				      	  Decimal
  resultatPrecedent  				      Decimal
  resultat     				      		  Decimal
}

model BonSortie {
  id              				      	BigInt    					           @id @default(autoincrement())
  numSortie       				      	Int
  codeSortie     				      	  String
  dateOperation  				      	  DateTime @db.Timestamp(0)
  mnt           				      	  Decimal
  employe       				      	  Employe     				      	  @relation(fields: [employeId], references: [id])
  employeId     				      	  BigInt
  repertoire    				      	  Repertoire  			            @relation(fields: [repertoireId], references: [id])
  repertoireId  				      	  BigInt
  detailBonSorties  				      DetailBonSortie[]
}

model BulttinPai {
  id              				      	BigInt    					           @id @default(autoincrement())
  codeBultin     				      	String
  observation   				      	String
  numbultin     				      	Int
  dateOperation  				      	DateTime @db.Timestamp(0)
  dateDebut     				      	DateTime @db.Date
  dateFin       				      	DateTime @db.Date
  idTypeBultinPai  				    	Int
  salairefx     				      	Decimal
  commission    				      	Decimal
  frais         				      	Decimal
  total         				      	Decimal
  totalMntVendue  				    	Decimal
  totalMntVendueProduit  		    	Decimal
  totalMntVendueLivraison  		  		Decimal
  mntNegative   				      	Decimal
  mntNegativeProduit  			    	Decimal
  mntNegativeLivraison  			  	Decimal
  mntCNSS       				      	Decimal
  mntPenalite   				      	Decimal
  mntBenefice   				      	Decimal
  commissionParProduit  			    Decimal
  primeSpecial  				      	Decimal
  fraisSupp     				      	Decimal
  primeCommercial  				    	Decimal
  externe       				      	Boolean
  totalMntVenduePrixCommercial  	  	Decimal
  totalMntVendueSansPrixCommercial  	Decimal
  primeProduit  				      	Decimal
  commercail    				      	Repertoire  			            @relation(fields: [commercailId], references: [id])
  commercailId  				      	BigInt
  employeOperateur  				    Employe     				      	@relation(fields: [employeOperateurId], references: [id])
  employeOperateurId  			    	BigInt
  bulttinPais      			      	  	DetBulttinPai[]
  detBulttinLivraisons  			    DetBulttinLivraison[]
}

model Caisse {
  id              				      	BigInt    					           @id @default(autoincrement())
  montant        				      	Decimal
  dateOperation  				      	DateTime @db.Date
  type          				      	Int
}

model Cheque {
  id              				      	BigInt    					           @id @default(autoincrement())
  numero        				      	String
  codeCheque     				      	String
  dateCheque     				      	DateTime @db.Date
  typeRepertoire  				    	Int
  montant       				      	Decimal
  typePersoScte  				    	Int
  numCheque     				      	Int
  etatcheque    				      	Boolean @default(false)
  typeReglment   				      	Int
  repertoire    				      	Repertoire  			            @relation(fields: [repertoireId], references: [id])
  repertoireId  				      	BigInt
  employeOperateur  				    Employe     				      	@relation(fields: [employeOperateurId], references: [id])
  employeOperateurId  			    	BigInt
}

model Compta {
  id              				      	BigInt    					           @id @default(autoincrement())
  dateDebut     				      	DateTime @db.Date
  dateFin       				      	DateTime @db.Date
  montantTVAPrecedent  			    	Decimal
  montantTVAAchat  				    	Decimal
  montantTVAVente  				    	Decimal
  resutMnt      				      	Decimal
  repertoire    				      	Repertoire  			            @relation(fields: [repertoireId], references: [id])
  repertoireId  				      	BigInt
}

model Comptabilite {
  id              				      	BigInt    					           @id @default(autoincrement())
  montantTTC     				      	Decimal
  montantTVA     				      	Decimal
  montantTVA0    				      	Decimal
  montantTVA7    				      	Decimal
  montantTVA10   				      	Decimal
  montantTVA14   				      	Decimal
  montantTVA20   				      	Decimal
  montantHT      				      	Decimal
  montantHT7     				      	Decimal
  montantHT10    				      	Decimal
  montantHT14    				      	Decimal
  montantHT20    				      	Decimal
  modeReglement  				    	String
  typeManuelAuto  				    	Int
  typeReglement  				    	Int
  dateFacture   				      	DateTime @db.Date
  dateRegelement  				    	DateTime @db.Date
  numFcture     				      	String
  montantDroitSupplementaire  			Decimal
  typeRepertoire  				    	Int
  repertoire    				      	Repertoire  			            @relation(fields: [repertoireId], references: [id])
  repertoireId  				      	BigInt
  employeOperateur  				    Employe     				      	@relation(fields: [employeOperateurId], references: [id])
  employeOperateurId  			    	BigInt
}

model CompteCaisse {
  id              				      	BigInt    					           @id @default(autoincrement())
  date          				      	DateTime @db.Date
  designation   				      	String
  numFacture    				      	String
  montant       				      	Decimal
  compteCaisse  				    	Boolean @default(false)
}

model DetAchatFacture {
  id              				      	BigInt    					           @id @default(autoincrement())
  unitegratuit   				      	Int
  qteacheter     				      	Int
  prixAchatHt    				      	Decimal
  prixAchatTtc   				      	Decimal
  remiseAchat    				      	Decimal
  prixVenteTtc   				      	Decimal
  mantantHt      				      	Decimal
  mantantTTC     				      	Decimal
  prixVenteAchatHT  				    Decimal
  beneficeDH     				      	Decimal
  benepourcentage  				    	Decimal
  tva20         				      	Decimal
  tva7          				      	Decimal
  achatFacture  				    	  AchatFacture  			        	@relation(fields: [achatFactureId], references: [id])
  achatFactureId  				    	BigInt
  stock         				      	Stock     				        	@relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model DetAchatLivraison {
  id              				      	BigInt    					           @id @default(autoincrement())
  unitegratuit   				      	Int
  qteacheter     				      	Int
  prixAchatHt    				      	Decimal
  prixAchatTtc   				      	Decimal
  remiseAchat    				      	Decimal
  prixVenteAchatHT  				    Decimal
  prixVenteTtc   				      	Decimal
  mantantHt      				      	Decimal
  mantantTTC     				      	Decimal
  tva7          				      	Decimal
  tva20         				      	Decimal
  tva14         				      	Decimal
  tva10         				      	Decimal
  beneficeDH     				      	Decimal
  benepourcentage  				    	Decimal
  achatLivraison  				    	AchatLivraison  			        @relation(fields: [achatLivraisonId], references: [id])
  achatLivraisonId  				    BigInt
  stock         				      	Stock     				        	@relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model DetailBonSortie {
  id              				      	BigInt    					           @id @default(autoincrement())
  qteSortie      				      	Int
  mntProduit     				      	Decimal
  total         				      	Decimal
  bonSortie     				      	BonSortie  				        	@relation(fields: [bonSortieId], references: [id])
  bonSortieId   				      	BigInt
  stock         				      	Stock     				        	@relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model DetBulttinLivraison {
  id              				      	BigInt    					           @id @default(autoincrement())
  commission     				      	Decimal
  commissionFixe  				    	Decimal
  commsiondh     				      	Decimal
  mantantcommission  				    Decimal
  benDH         				      	Decimal
  rougenormal   				      	Boolean
  bulttinPai    				      	BulttinPai  			          	@relation(fields: [bulttinPaiId], references: [id])
  bulttinPaiId  				    	BigInt
  livraison     				      	Livraison  				        	@relation(fields: [livraisonId], references: [id])
  livraisonId   				      	BigInt
}

model DetBulttinPai {
  id              				      	BigInt    					           @id @default(autoincrement())
  prixlivraison  				    	Decimal
  qtevendu      				      	Int
  prixvente     				      	Decimal
  remise        				      	Decimal
  mantantvendu   				      	Decimal
  commission     				      	Decimal
  commsiondh     				      	Decimal
  mantant       				      	Decimal
  benDH         				      	Decimal
  commissionFixe  				    	Decimal
  prixCommercial  				    	Decimal
  primeCommercial  				    	Decimal
  primeProduit  				    	Decimal
  avecZero      				      	Boolean @default(false)
  bulttinPai    				      	BulttinPai  			          	@relation(fields: [bulttinPaiId], references: [id])
  bulttinPaiId  				    	BigInt
  produit       				      	Stock     				        	@relation(fields: [produitId], references: [id])
  produitId     				      	BigInt
}

model DetFacture {
  id              				      	BigInt    					           @id @default(autoincrement())
  qteFacturer    				      	Int
  remiseFacture  				    	  Decimal
  prixVente     				      	Decimal
  montantProduit  				    	Decimal
  beneficeDH     				      	Decimal
  benepourcentage  				    	Decimal
  tva7          				      	Decimal
  tva20         				      	Decimal
  montantProduitHT  				    Decimal
  directFacture  				    	  Int
  avecRemise    				      	Boolean
  facture       				      	Facture   				        	  @relation(fields: [factureId], references: [id])
  factureId     				      	BigInt
  stock         				      	Stock     				        	  @relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model DetImportations {
  id              				      BigInt    					          @id @default(autoincrement())
  qteAchat      				      	Int
  qteStock      				      	Int
  prixAchat     				      	Decimal
  importations  				    	  Importations  			        	@relation(fields: [importationsId], references: [id])
  importationsId  				    	BigInt
  stock         				      	Stock     				        	  @relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model DetLivraison {
  id              				      BigInt    					           @id @default(autoincrement())
  qteLivrer      				      	Int
  champsRouge   				      	Int
  remiseLivraison  				    	Decimal
  prixVente     				      	Decimal
  montantProduit  				    	Decimal
  beneficeDH     				      	Decimal
  benepourcentage  				    	Decimal
  infinity      				      	Int
  avecRemise    				      	Boolean
  livraison     				      	Livraison  				        	@relation(fields: [livraisonId], references: [id])
  livraisonId   				      	BigInt
  stock         				      	Stock     				        	@relation(fields: [stockId], references: [id])
  stockId       				      	BigInt
}

model Facture {
  id              				      BigInt    					           @id @default(autoincrement())
  numFacture     				      	Int
  codeBF        				      	String
  dateBF        				      	DateTime @db.Date
  dateReglement  				    	  DateTime @db.Date
  dateReglement2  				    	DateTime @db.Date
  dateReglement3  				    	DateTime @db.Date
  dateReglement4  				    	DateTime @db.Date
  typeReglment   				      	Int
  typeReglment2  				    	  Int
  typeReglment3  				    	  Int
  typeReglment4  				    	  Int
  typePaiement   				      	String
  mantantBF      				      	Decimal
  mantantBFBenefice  				    Decimal
  tva7Total     				      	Decimal
  tva20Total    				      	Decimal
  sysDate       				      	DateTime
  mantantBFHT   				      	Decimal
  numeroLibil   				      	String
  mntHT2O       				      	Decimal
  mntHT7        				      	Decimal
  tva7          				      	Decimal
  tva20         				      	Decimal
  numCheque     				      	String
  numCheque2    				      	String
  numCheque3    				      	String
  numCheque4    				      	String
  numRemise     				      	String
  numRemise2    				      	String
  numRemise3    				      	String
  numRemise4    				      	String
  mntReglement  				    	  Decimal
  mntReglement2  				    	  Decimal
  mntReglement3  				    	  Decimal
  mntReglement4  				    	  Decimal
  type          				      	Int
  facturer100   				      	Boolean @default(false)
  calculer      				      	Boolean @default(false)
  disableMontant 				        Boolean @default(false)
  typeTVA       				      	String
  dateReglementIn 				      	Boolean? @default(false)
  dateReglement2In 			      		Boolean? @default(false)
  dateReglement3In 			      		Boolean? @default(false)
  dateReglement4In 			      		Boolean? @default(false)
  tva20Reglement1  				    	Decimal
  tva20Reglement2  				    	Decimal
  tva20Reglement3  				    	Decimal
  tva20Reglement4  				    	Decimal
  mntHT20Reglement1  				    Decimal
  mntHT20Reglement2  				    Decimal
  detFactures  				    		DetFacture[]
}

model Importations {
  id              				      	BigInt    					           @id @default(autoincrement())
  codeFacture    				    	String
  devisEuroDolar  				    	String
  numFacture     				      	Int
  dateFacture   				      	DateTime @db.Date
  sysDate       				      	DateTime
  dateAvances1  				    	DateTime @db.Date
  mantantAvancs1  				    	Decimal
  dateAvances2  				    	DateTime @db.Date
  mantantAvancs2  				    	Decimal
  totalPaye     				      	Decimal
  mntFacture    				    	Decimal
  mntDouane     				      	Decimal
  mntTransport  				    	Decimal
  mntTransportIntern  				    Decimal
  mntTransit    				    	Decimal
  mntMagasinage  				    	Decimal
  prixAchatDetaille  				    Decimal
  totalAllMnt   				      	Decimal
  repertoireFour  				    	Repertoire  			            @relation(fields: [repertoireFourId], references: [id])
  repertoireFourId  				    BigInt
  employeOperateur  				    Employe     				      	@relation(fields: [employeOperateurId], references: [id])
  employeOperateurId  			    	BigInt
  detImportations    				    DetImportations[]
}

model Livraison {
  id              				      	BigInt    					           @id @default(autoincrement())
  numLivraison   				      	Int
  codeBl        				      	String
  dateBl        				      	DateTime @db.Date
  dateReglement  				    	DateTime @db.Date
  dateReglement2  				    	DateTime @db.Date
  dateReglement3  				    	DateTime @db.Date
  dateReglement4  				    	DateTime @db.Date
  typeReglment   				      	Int
  typeReglment2  				    	Int
  typeReglment3  				    	Int
  typeReglment4  				    	Int
  mantantBL      				      	Decimal
  mantantBLReel  				    	Decimal
  mantantBLBenefice  				    Decimal
  typePaiement   				      	String
  mantantBLPourcent  				    Decimal
  reglerNonRegler  				    	Int
  sysDate       				      	DateTime
  infinity      				      	Int
  etatBultinPaie  				    	Int
  livrernonlivrer  				    	Int
  avecRemise    				      	Boolean
  mntReglement  				    	Decimal
  mntReglement2  				    	Decimal
  mntReglement3  				    	Decimal
  mntReglement4  				    	Decimal
  facturer100   				      	Boolean @default(false)
  codeTransport  				    	String
  employeOperateur  				    Employe     				      	@relation(fields: [employeOperateurId], references: [id])
  employeOperateurId  			    	BigInt
  repertoireByCommercial  		    	Repertoire  			            @relation("commercialLivraison", fields: [repertoireByCommercialId], references: [id])
  repertoireByCommercialId  		    BigInt
  repertoireByCommercialAncien  		Repertoire?  			            @relation("commercialAncienLivraison", fields: [repertoireByCommercialAncienId], references: [id])
  repertoireByCommercialAncienId  		BigInt
  repertoireByClient  			        Repertoire  			            @relation("clientLivraison", fields: [repertoireByClientId], references: [id])
  repertoireByClientId  				BigInt
  detLivraisons  				    	DetLivraison[]
  detBulttinLivraisons  			    DetBulttinLivraison[]
}

model PlanComptable {
  id              				      	BigInt    					           @id @default(autoincrement())
  code          				      	Int
  designation   				      	String
  nature        				      	String
  formule       				      	String
  codeString    				      	String
  balances      				      	Balance[]
  soldes  				    			Solde[]
}

model Solde {
  id              				      	BigInt    					           @id @default(autoincrement())
  dateOperation  				    	DateTime @db.Date
  dateReglement  				    	DateTime @db.Date
  debit         				      	Decimal
  credit        				      	Decimal
  solde         				      	Decimal
  nature        				      	String
  planComptable  				      	PlanComptable  			        @relation(fields: [planComptableId], references: [id])
  planComptableId  			      		BigInt
}

enum TypePersonnel {
  NONE                        @map("0")
  ADMINISTRATEUR              @map("1")
  GERANT                      @map("2")
  COMPATBLE                   @map("3")
  MAGASINIER                  @map("4")
  COMMERCIAL_INTERNE          @map("5")
  COMMERCIAL_EXTERNE          @map("6")
}

enum TypeRepertoire {
  NONE                        @map("0")
  EMPLOYE                     @map("1")
  PHARMACIE                   @map("2")
  FOURNISSEUR                 @map("3")
  REVENDEUR                   @map("4")
}